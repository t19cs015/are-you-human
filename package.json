{
  "name": "ai-town",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "build": "node scripts/build-web.mjs",
    "test": "node --test tests/*.test.mjs",
    "build:surfaces": "node scripts/build-surfaces.mjs",
    "build:community-voices": "node scripts/build-community-voices.mjs",
    "build:voices": "node scripts/build-episode-voices.mjs"
  },
  "dependencies": {
    "@vercel/functions": "3.9.7",
    "polygon-clipping": "0.15.7",
    "three": "0.170.0"
  },
  "engines": {
    "node": "22.x"
  }
}
